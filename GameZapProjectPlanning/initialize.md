## **What Is It**
- A function that gets executed at the start of the program for each enabled component
	- Also gets called when non-initialized components are enabled for the first time (e.g. a component starts the program as disabled, then gets enabled)
	- Does not execute for repeated enabling of a component